# FASTAPI & AGENT ENVIRONMENT CONFIGURATION
SERVER_HOST=127.0.0.1
SERVER_PORT=8000
FASTAPI_ENV=development
API_KEY=test-dev-secret-key-ps26171

# LLM / VLM PROVIDER (Choices: mock, cloud, local)
LLM_PROVIDER=mock
VLM_PROVIDER=mock

# CLOUD PROVIDER CONFIG (Optional: required only if LLM_PROVIDER=cloud)
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GEMINI_API_KEY=
GROQ_API_KEY=

# PRIVACY ENGINE DEFAULTS
FAIL_CLOSED=true
CONFIDENCE_THRESHOLD=0.75
REDACTION_PADDING_PX=4
DATA_BUDGET_GB=139.5
RANDOM_SEED=42
